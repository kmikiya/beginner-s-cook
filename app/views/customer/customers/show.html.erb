<div class="container">
  <div>
    <%= attachment_image_tag @customer, :bg_image, :fill, 200, 200, fallback:"no_bg_image.jpg"%>
  </div>
  <div>
    <%= attachment_image_tag @customer, :profile_image, :fill, 200, 200, fallback:"no_bg_image.jpg", size: '150x150'%>
    <%= @customer.name %>
    <%= @customer.profile %>
    <% if current_customer == @customer %>
      <%= link_to "プロフィール編集", edit_customer_path(@customer) %>
    <% end %>
  </div>



</div>
